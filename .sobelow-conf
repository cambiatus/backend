[
  verbose: false,
  private: false,
  skip: true,
  router: "",
  exit: "true",
  format: "txt",
  out: "",
  threshold: "low",
  # HTTPS handling is already done by NGINX
  # Database handling is handled by Ecto, which also handles injections
  ignore: ["Config.HTTPS", "SQL.Query"],
  ignore_files: [""]
]
