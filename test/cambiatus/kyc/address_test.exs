defmodule Cambiatus.AddressTest do
  use Cambiatus.DataCase

  test "changeset is fine with correct data" do
  end

  test "changeset is invalid with invalid country" do
  end

  test "changeset is invalid with invalid zip code" do
  end
end
