defmodule BeSpiral.NotificationHistoryTest do
  use BeSpiral.DataCase

  describe "notification_history" do
    test "collects a notification when one exists"
    test "collects all notifications for a user"
  end
end
